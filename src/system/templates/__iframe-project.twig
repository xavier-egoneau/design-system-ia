<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>{{ componentName }} - Isolé</title>
  
  {# Framework CSS (si défini) #}
  {% if framework.css is defined %}
    <link href="{{ framework.css }}" rel="stylesheet">
  {% endif %}
  
  <link rel="stylesheet" href="/css/main.css">
  
  <style>
    body {
      margin: 0;
      padding: 2rem;
      min-height: calc(100vh - 4rem);
      display: flex;
      align-items: center;
      justify-content: center;
      background: white;
    }
    
    #component-render {
      max-width: 100%;
    }
    
    /* Styles adaptatifs selon le framework */
    {% if framework.name == 'bootstrap' %}
    .alert {
      margin: 1rem;
      max-width: 500px;
    }
    
    .btn {
      margin: 0.5rem;
    }
    
    .card {
      max-width: 400px;
      margin: 1rem;
    }
    {% elseif framework.name == 'dsfr' %}
    .fr-btn {
      margin: 0.5rem;
    }
    
    .fr-card {
      max-width: 400px;
      margin: 1rem;
    }
    {% endif %}
  </style>
</head>
<body>
  <div id="component-render">
    {{ content|raw }}
  </div>
  
  {# Framework JS (si défini) #}
  {% if framework.js is defined %}
    <script src="{{ framework.js }}"></script>
  {% endif %}
  
  <script src="/js/project-iframe.js"></script>
</body>
</html>